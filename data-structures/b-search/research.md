# Binary Search

## Things to note
- if lower bound > upper bound = base case
- List must be sorted
- maintain state by lower/upper bound
- check median
    - median is determined by upper bound + lower bound (median = lowerBound + (upper - lower) / 2)
- time complexity of O(logn) - list is getting smaller as search continues

